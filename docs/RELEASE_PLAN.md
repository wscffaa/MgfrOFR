# Release Plan

This repository is intentionally code-free before paper acceptance.

After acceptance, the public release should add:

1. source code;
2. training and testing configs;
3. pretrained weights with SHA256 checksums;
4. dataset preparation instructions;
5. reproduction commands;
6. smoke tests;
7. final benchmark tables with metric provenance.

The private implementation package is maintained separately until release is allowed.
